@model IEnumerable<Proyecto.Models.Receta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RecetaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductoId)
        </th>
        <th>
            Ids de materias primas
        </th>
        <th>
            Cantidades
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.RecetaId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ProductoId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MateriasPrimas)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Cantidades)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.RecetaId })
        @Html.ActionLink("Details", "Details", new { id = item.RecetaId })
        @Html.ActionLink("Delete", "Delete", new { id = item.RecetaId })
    </td>
</tr>
    }

</table>
